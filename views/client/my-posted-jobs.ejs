<!-- views/client/my-posted-jobs.ejs -->
<!DOCTYPE html>
<html lang="en">
<head>
    <%- include('../partials/head', { title: title }) %>
    <link rel="stylesheet" href="/css/style.css">
</head>
<body class="dashboard-body">
    <div class="dashboard-wrapper">
        <%- include('../partials/sidebar', { user: user, active: active }) %>
        <main class="dashboard-main-content">
            <div class="page-header glass-effect">
                <h1><i class="fas fa-briefcase icon"></i> Pekerjaan yang Saya Posting</h1>
            </div>
            <div class="content-table glass-effect">
                <table>
                    <thead>
                        <tr>
                            <th>Judul</th>
                            <th>Tanggal Posting</th>
                            <th>Proposal Masuk</th>
                            <th>Status</th>
                            <th>Aksi</th>
                        </tr>
                    </thead>
                    <tbody>
                        <% if (jobs && jobs.length > 0) { %>
                            <% jobs.forEach(job => { %>
                                <tr>
                                    <td><%= job.title %></td>
                                    <td><%= new Date(job.createdAt).toLocaleDateString('id-ID') %></td>
                                    <td><%= job.proposals.length %></td>
                                    <td><span class="status-<%= job.status.replace('-','') %>"><%= job.status %></span></td>
                                    <td><a href="/jobs/<%= job._id %>" class="btn btn-small btn-secondary">Lihat Detail</a></td>
                                </tr>
                            <% }) %>
                        <% } else { %>
                            <tr>
                                <td colspan="5">Anda belum memposting pekerjaan apapun.</td>
                            </tr>
                        <% } %>
                    </tbody>
                </table>
            </div>
        </main>
    </div>
</body>
</html>